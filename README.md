# data-quality-pipeline
A modular Python/Pandas ETL pipeline transforming raw energy metering data into analytics-ready datasets. Features a 3-layer architecture (raw → cleaned → curated), 8+ automated data quality checks, duplicate detection, and outer-join logic ensuring no records are lost during merging. Includes unit tests and centralized config.
